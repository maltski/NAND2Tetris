/** Initializes a new Traffic Racer game and starts running it. */
class Main {
    function void main() {
        var TrafficRacerGame game;
        let game = TrafficRacerGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}